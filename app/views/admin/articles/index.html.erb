<h1>Listing admin_articles</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Material</th>
    <th>Little pic</th>
    <th>Category</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_articles.each do |admin_article| %>
  <tr>
    <td><%= admin_article.name %></td>
    <td><%= admin_article.description %></td>
    <td><%= admin_article.material %></td>
    <td><%= admin_article.little_pic %></td>
    <td><%= admin_article.category %></td>
    <td><%= admin_article.price %></td>
    <td><%= link_to 'Show', [:admin,admin_article] %></td>
    <td><%= link_to 'Edit', edit_admin_article_path(admin_article) %></td>
    <td><%= link_to 'Destroy', [:admin,admin_article], :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Article', new_admin_article_path %>
